{
  "name": "web-scraper",
  "version": "1.0.0",
  "description": "Basic Web Scraper Implementation: Build a simple Node.js scraper using either Puppeteer or Playwright. Target a sample website (e.g., quotes.toscrape.com, or any job board, e-commerce site). Extract structured data (title, price, image URL, etc.). Output to console or save as JSON/CSV.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^24.12.1"
  }
}
